Sys.setenv(RETICULATE_PYTHON = "C:/Users/deli/Documents/.virtualenvs/keras_env/Scripts/python.exe")
library(reticulate)
use_virtualenv("keras_env", required = TRUE)
py_discover_config()
message("✔ Ambiente keras_env attivato")

# 
# Questo è un avviso (warning) che ti segnala che una funzione specifica 
# (in questo caso, tf.losses.sparse_softmax_cross_entropy) è deprecata e che 
# dovresti usare la versione compatibile, 
# tf.compat.v1.losses.sparse_softmax_cross_entropy.
